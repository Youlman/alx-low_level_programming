#include <stdio.h>
#include <stdlib.h>

/**
* main - Print the program name
*
* Return: Always 0.
*/
int main(int __attribute__((__unused__)) argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
